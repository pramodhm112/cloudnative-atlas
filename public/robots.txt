# CloudNative Atlas - robots.txt
# Allows all search engines to crawl the site

User-agent: *
Allow: /

# Block crawling of API endpoints (optional, they're already JSON)
# Disallow: /api/

# Sitemap location
Sitemap: https://cloudnativeatlas.com/sitemap-index.xml

# Crawl delay (optional, only if you experience heavy bot traffic)
# Crawl-delay: 1
